---
phase: 02-market-data-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quant_team/market/crypto_data.py
  - quant_team/market/router.py
  - quant_team/teams/registry.py
  - tests/test_crypto_data.py
  - tests/test_router.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "CryptoMarketData fetches Solana token prices from Jupiter Price API v3"
    - "CryptoMarketData fetches OHLCV candlestick data from a CEX via ccxt"
    - "CryptoMarketData returns a DataFrame compatible with compute_all() from indicators.py"
    - "MarketDataRouter dispatches to StockMarketData for stocks asset class"
    - "MarketDataRouter dispatches to CryptoMarketData for crypto asset class"
    - "MarketDataRouter raises ValueError for unknown asset classes"
  artifacts:
    - path: "quant_team/market/crypto_data.py"
      provides: "CryptoMarketData class with Jupiter + ccxt"
      contains: "class CryptoMarketData"
    - path: "quant_team/market/router.py"
      provides: "MarketDataRouter dispatch"
      contains: "class MarketDataRouter"
    - path: "tests/test_crypto_data.py"
      provides: "Unit tests for CryptoMarketData"
      contains: "test_jupiter_price_fetch"
    - path: "tests/test_router.py"
      provides: "Unit tests for MarketDataRouter"
      contains: "test_stocks_routes_to_stock_provider"
  key_links:
    - from: "quant_team/market/router.py"
      to: "quant_team/market/crypto_data.py"
      via: "import CryptoMarketData"
      pattern: "from \\.crypto_data import CryptoMarketData"
    - from: "quant_team/market/router.py"
      to: "quant_team/market/stock_data.py"
      via: "import StockMarketData"
      pattern: "from \\.stock_data import StockMarketData"
---

<objective>
Build the CryptoMarketData provider and MarketDataRouter dispatch layer — the two new modules that enable asset-class-aware market data fetching.

Purpose: This is the core data abstraction for Phase 2. CryptoMarketData wraps Jupiter Price API v3 (Solana token prices) and ccxt (CEX OHLCV/tickers). MarketDataRouter dispatches to the correct provider based on TeamConfig.asset_class. Tests verify all contracts with mocked external calls.

Output: Two new production modules (crypto_data.py, router.py), updated TeamConfig with exchange field, and two test files covering all DATA requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-data-routing/02-RESEARCH.md

@quant_team/market/stock_data.py
@quant_team/market/indicators.py
@quant_team/teams/registry.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From quant_team/teams/registry.py:
```python
@dataclass
class TeamConfig:
    team_id: str
    name: str
    asset_class: str  # "stocks" or "crypto"
    agents: list[AgentSpec] = field(default_factory=list)
    risk_limits: RiskLimits = field(default_factory=RiskLimits)
    schedule_cron: list[dict] = field(default_factory=list)
    execution_backend: str = "paper"
    watchlist: list[str] = field(default_factory=list)
```

From quant_team/market/stock_data.py (interface CryptoMarketData must match):
```python
class StockMarketData:
    def __init__(self, cache_ttl: int = 60): ...
    def fetch_ohlcv(self, ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame: ...
    def fetch_quote(self, ticker: str) -> dict: ...
    def fetch_multiple_quotes(self, tickers: list[str]) -> dict[str, dict]: ...
    def get_market_summary(self, tickers: list[str]) -> str: ...
    def get_options_summary(self, ticker: str) -> str: ...
    # Also used by PortfolioManager: fetch_quote(), fetch_options_chain()
    def fetch_options_chain(self, ticker: str) -> dict: ...
```

From quant_team/market/indicators.py:
```python
def compute_all(df: pd.DataFrame) -> str:
    # Expects df with columns: "close", "open", "high", "low", "volume"
    # Index must be DatetimeIndex
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CryptoMarketData provider with Jupiter + ccxt</name>
  <files>quant_team/market/crypto_data.py, tests/test_crypto_data.py</files>
  <read_first>
    - quant_team/market/stock_data.py (interface contract to match)
    - quant_team/market/indicators.py (compute_all expects specific DataFrame columns)
    - .planning/phases/02-market-data-routing/02-RESEARCH.md (Jupiter API v3 details, ccxt patterns)
  </read_first>
  <behavior>
    - test_jupiter_price_fetch: mock httpx.get to return {"data": {"So11...": {"price": "150.5"}}}, call fetch_jupiter_prices(["So11..."]), assert returns {"So11...": 150.5}
    - test_jupiter_missing_key: with JUPITER_API_KEY unset, fetch_jupiter_prices returns {} (graceful degradation, no crash)
    - test_jupiter_http_error: mock httpx.get to raise, fetch_jupiter_prices returns {} (graceful degradation)
    - test_ccxt_ohlcv_dataframe: mock exchange.fetch_ohlcv to return [[ts, 100, 105, 95, 102, 5000], ...] (10 rows), call fetch_ohlcv("SOL"), assert returns DataFrame with columns ["open", "high", "low", "close", "volume"] and DatetimeIndex
    - test_ccxt_ticker_quote: mock exchange.fetch_ticker to return {"last": 150.0, "bid": 149.5, "ask": 150.5, "quoteVolume": 1000000}, call fetch_quote("SOL"), assert returns dict with "ticker", "price" keys
    - test_fetch_quote_returns_expected_shape: fetch_quote returns dict with at least keys "ticker" and "price"
    - test_get_market_summary_returns_string: get_market_summary(["SOL"]) returns non-empty str
    - test_get_options_summary_returns_empty: get_options_summary("SOL") returns empty string (crypto has no options)
    - test_fetch_options_chain_returns_empty: fetch_options_chain("SOL") returns dict with empty chains
    - test_ticker_to_mint: ticker_to_mint("SOL") returns "So11111111111111111111111111111111111111112", ticker_to_mint("UNKNOWN") returns None
    - test_ticker_to_ccxt_pair: ticker_to_ccxt_pair("SOL") returns "SOL/USDT"
  </behavior>
  <action>
    Create `quant_team/market/crypto_data.py` with:

    1. Module docstring: `"""Crypto market data provider using Jupiter Price API v3 and ccxt."""`
    2. `from __future__ import annotations` as first import
    3. Standard imports: `import logging`, `import os`, `import time`
    4. Third-party: `import ccxt`, `import httpx`, `import pandas as pd`
    5. `logger = logging.getLogger("quant_team")`

    6. Constants:
       - `JUPITER_PRICE_URL = "https://api.jup.ag/price/v3/price"`
       - `KNOWN_MINTS` dict mapping: SOL -> So11111111111111111111111111111111111111112, USDC -> EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v, JUP -> JUPyiwrYJFskUPiHa7hkeR8VUtAeFoSYbKedZNsDvCN, BONK -> DezXAZ8z7PnrnRJjz3wXBoRgixCa6xjnB7YaB1pPB263

    7. Helper functions:
       - `ticker_to_mint(symbol: str) -> str | None`: returns KNOWN_MINTS.get(symbol.upper())
       - `ticker_to_ccxt_pair(symbol: str, quote: str = "USDT") -> str`: returns f"{symbol.upper()}/{quote}"
       - `fetch_jupiter_prices(mints: list[str]) -> dict[str, float]`: synchronous httpx.get to JUPITER_PRICE_URL with params={"ids": ",".join(mints)}, headers={"X-API-Key": os.environ.get("JUPITER_API_KEY", "")}. timeout=10.0. Wrap in try/except Exception, return {} on failure. Log warning if JUPITER_API_KEY is empty. Parse response: {"data": {mint: {"price": "..."}}} -> {mint: float(price)}.

    8. `class CryptoMarketData`:
       - Docstring: `"""Fetches crypto market data via ccxt (CEX) and Jupiter Price API (Solana DeFi)."""`
       - `__init__(self, exchange_name: str = "binance", cache_ttl: int = 60)`:
         - `self.exchange = getattr(ccxt, exchange_name)()` — instantiate ccxt exchange by name
         - `self.cache_ttl = cache_ttl`
         - `self._quote_cache: dict[str, tuple[float, dict]] = {}`
         - `self._ohlcv_cache: dict[str, tuple[float, pd.DataFrame]] = {}`

       - `fetch_ohlcv(self, ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame`:
         - Check cache first (same pattern as StockMarketData)
         - Map period to ccxt limit: {"1mo": 30, "3mo": 90, "6mo": 180, "1y": 365}.get(period, 90)
         - Map interval to ccxt timeframe: {"1d": "1d", "1h": "1h", "5m": "5m"}.get(interval, "1d")
         - Call `self.exchange.fetch_ohlcv(ticker_to_ccxt_pair(ticker), timeframe=tf, limit=limit)`
         - Convert to DataFrame with columns ["timestamp", "open", "high", "low", "close", "volume"]
         - `df["timestamp"] = pd.to_datetime(df["timestamp"], unit="ms")`
         - `df.set_index("timestamp", inplace=True)`
         - Cache and return df
         - On any exception: raise ValueError(f"No OHLCV data for {ticker}: {e}")

       - `fetch_quote(self, ticker: str) -> dict`:
         - Check cache first
         - Try Jupiter first: call fetch_jupiter_prices with the mint for this ticker
         - If Jupiter returns a price, build quote dict from it
         - Fallback to ccxt ticker: self.exchange.fetch_ticker(ticker_to_ccxt_pair(ticker))
         - Return dict matching StockMarketData shape: {"ticker": ticker, "price": float, "previous_close": 0.0, "open": 0.0, "day_high": 0.0, "day_low": 0.0, "volume": int, "market_cap": 0.0, "change_pct": 0.0}
         - For ccxt ticker, populate price from ticker["last"], volume from ticker["quoteVolume"]
         - Cache and return
         - On exception: return {"ticker": ticker, "price": 0.0, ...} with all zeros (graceful degradation)

       - `fetch_multiple_quotes(self, tickers: list[str]) -> dict[str, dict]`:
         - Same pattern as StockMarketData: loop with try/except per ticker

       - `fetch_options_chain(self, ticker: str) -> dict`:
         - Return `{"ticker": ticker, "expirations": [], "chains": {}}` (crypto has no options in Phase 2)

       - `get_market_summary(self, tickers: list[str]) -> str`:
         - Format similar to StockMarketData but crypto-appropriate
         - Header: "# Crypto Market Summary"
         - Section "## Token Prices" — loop tickers, fetch_quote each, format price/volume
         - No sector ETFs or market indices (those are stock-specific)
         - Wrap each ticker fetch in try/except, show "unavailable" on failure

       - `get_options_summary(self, ticker: str) -> str`:
         - Return `""` (empty string — crypto has no options chains)

    Create `tests/test_crypto_data.py` with all behavior tests listed above. Use `unittest.mock.patch` to mock `httpx.get` for Jupiter tests and `ccxt.binance` for ccxt tests. Use `monkeypatch.setenv`/`monkeypatch.delenv` for JUPITER_API_KEY tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_crypto_data.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class CryptoMarketData" quant_team/market/crypto_data.py
    - grep -q "JUPITER_PRICE_URL" quant_team/market/crypto_data.py
    - grep -q "api.jup.ag/price/v3/price" quant_team/market/crypto_data.py
    - grep -q "KNOWN_MINTS" quant_team/market/crypto_data.py
    - grep -q "ticker_to_mint" quant_team/market/crypto_data.py
    - grep -q "ticker_to_ccxt_pair" quant_team/market/crypto_data.py
    - grep -q "fetch_jupiter_prices" quant_team/market/crypto_data.py
    - grep -q "get_options_summary" quant_team/market/crypto_data.py
    - grep -q "fetch_options_chain" quant_team/market/crypto_data.py
    - grep -q "def test_jupiter_price_fetch" tests/test_crypto_data.py
    - grep -q "def test_ccxt_ohlcv_dataframe" tests/test_crypto_data.py
    - grep -q "def test_get_options_summary_returns_empty" tests/test_crypto_data.py
    - python -m pytest tests/test_crypto_data.py -x -v exits 0
  </acceptance_criteria>
  <done>CryptoMarketData class exists with Jupiter + ccxt integration, matching StockMarketData interface. All 11 tests pass with mocked external calls. get_options_summary returns empty string. fetch_options_chain returns empty dict structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MarketDataRouter and add exchange field to TeamConfig</name>
  <files>quant_team/market/router.py, quant_team/teams/registry.py, tests/test_router.py</files>
  <read_first>
    - quant_team/market/stock_data.py (StockMarketData interface)
    - quant_team/market/crypto_data.py (CryptoMarketData just created in Task 1)
    - quant_team/teams/registry.py (TeamConfig dataclass to extend)
    - quant_team/orchestrator.py (lines 14, 48 — current StockMarketData usage)
    - quant_team/trading/portfolio_manager.py (lines 13, 19 — type hint on StockMarketData)
  </read_first>
  <behavior>
    - test_stocks_routes_to_stock_provider: create TeamConfig(team_id="t", name="T", asset_class="stocks"), create MarketDataRouter(config), assert router._provider is instance of StockMarketData
    - test_crypto_routes_to_crypto_provider: create TeamConfig(team_id="t", name="T", asset_class="crypto"), create MarketDataRouter(config), assert router._provider is instance of CryptoMarketData
    - test_unknown_asset_class_raises: create TeamConfig(team_id="t", name="T", asset_class="options"), assert MarketDataRouter(config) raises ValueError containing "Unknown asset_class"
    - test_router_delegates_fetch_quote: mock provider.fetch_quote, call router.fetch_quote("AAPL"), assert provider.fetch_quote was called with "AAPL"
    - test_router_delegates_fetch_ohlcv: mock provider.fetch_ohlcv, call router.fetch_ohlcv("AAPL"), assert delegated
    - test_router_delegates_get_market_summary: mock provider.get_market_summary, call router.get_market_summary(["AAPL"]), assert delegated
    - test_router_delegates_get_options_summary: mock provider.get_options_summary, call router.get_options_summary("AAPL"), assert delegated
    - test_router_delegates_fetch_options_chain: mock provider.fetch_options_chain, call router.fetch_options_chain("AAPL"), assert delegated
    - test_router_delegates_fetch_multiple_quotes: mock provider.fetch_multiple_quotes, call router.fetch_multiple_quotes(["AAPL"]), assert delegated
    - test_team_config_exchange_field: TeamConfig(team_id="c", name="C", asset_class="crypto", exchange="kraken") has exchange == "kraken"
    - test_team_config_exchange_default: TeamConfig(team_id="c", name="C", asset_class="crypto") has exchange == "binance"
    - test_crypto_router_uses_exchange_from_config: create TeamConfig with exchange="kraken", create MarketDataRouter, assert provider.exchange.id == "kraken"
  </behavior>
  <action>
    **Step 1: Add `exchange` field to TeamConfig in `quant_team/teams/registry.py`:**
    - Add `exchange: str = "binance"` field to the TeamConfig dataclass, after `execution_backend`
    - In `TeamRegistry._parse()`, add: `exchange=data.get("exchange", "binance")` to the TeamConfig constructor call

    **Step 2: Create `quant_team/market/router.py`:**
    - Module docstring: `"""Market data routing — dispatches to asset-class-specific providers."""`
    - `from __future__ import annotations`
    - Imports: `from .stock_data import StockMarketData`, `from .crypto_data import CryptoMarketData`, `from ..teams.registry import TeamConfig`
    - `import pandas as pd`

    - `class MarketDataRouter`:
      - Docstring: `"""Routes market data fetches to the correct provider based on asset class."""`
      - `__init__(self, config: TeamConfig)`:
        - `self.config = config`
        - if config.asset_class == "stocks": self._provider = StockMarketData()
        - elif config.asset_class == "crypto": self._provider = CryptoMarketData(exchange_name=config.exchange)
        - else: raise ValueError(f"Unknown asset_class: {config.asset_class!r}")

      - Delegation methods (each just calls self._provider.method):
        - `fetch_ohlcv(self, ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame`
        - `fetch_quote(self, ticker: str) -> dict`
        - `fetch_multiple_quotes(self, tickers: list[str]) -> dict[str, dict]`
        - `fetch_options_chain(self, ticker: str) -> dict`
        - `get_market_summary(self, tickers: list[str]) -> str`
        - `get_options_summary(self, ticker: str) -> str`

    **Step 3: Create `tests/test_router.py`:**
    - All behavior tests listed above
    - Use plain TeamConfig construction (no YAML needed) for test configs
    - Mock CryptoMarketData.__init__ where needed to avoid ccxt exchange instantiation in pure routing tests

    Ensure `from __future__ import annotations` is first import in every new file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_router.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class MarketDataRouter" quant_team/market/router.py
    - grep -q "exchange: str" quant_team/teams/registry.py
    - grep -q "def fetch_quote" quant_team/market/router.py
    - grep -q "def fetch_ohlcv" quant_team/market/router.py
    - grep -q "def get_market_summary" quant_team/market/router.py
    - grep -q "def get_options_summary" quant_team/market/router.py
    - grep -q "def fetch_options_chain" quant_team/market/router.py
    - grep -q "def fetch_multiple_quotes" quant_team/market/router.py
    - grep -q "def test_stocks_routes_to_stock_provider" tests/test_router.py
    - grep -q "def test_crypto_routes_to_crypto_provider" tests/test_router.py
    - grep -q "def test_unknown_asset_class_raises" tests/test_router.py
    - python -m pytest tests/test_router.py -x -v exits 0
    - python -m pytest tests/test_crypto_data.py tests/test_router.py tests/test_registry.py -x -v exits 0 (no regressions)
  </acceptance_criteria>
  <done>MarketDataRouter dispatches to StockMarketData for "stocks" and CryptoMarketData for "crypto". TeamConfig has exchange field defaulting to "binance". All router delegation methods work. All 12 router tests pass. Existing registry tests still pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m pytest tests/test_crypto_data.py tests/test_router.py tests/test_registry.py -x -v` — all tests green
2. `python -m pytest tests/ -x -q` — full suite green, no regressions
3. `grep -r "class CryptoMarketData" quant_team/market/crypto_data.py` — exists
4. `grep -r "class MarketDataRouter" quant_team/market/router.py` — exists
5. `grep "exchange: str" quant_team/teams/registry.py` — field exists
</verification>

<success_criteria>
- CryptoMarketData exists with Jupiter Price API v3 + ccxt integration
- MarketDataRouter dispatches based on TeamConfig.asset_class
- Both match StockMarketData's public interface (fetch_quote, fetch_ohlcv, get_market_summary, get_options_summary, fetch_options_chain, fetch_multiple_quotes)
- TeamConfig has exchange field with "binance" default
- All new tests pass, no regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-data-routing/02-01-SUMMARY.md`
</output>
